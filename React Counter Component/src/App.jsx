// useState() hook = A react hook that allow the creation of a stateful variable
// And a setter function to update its value in the virtual DOM [name, setName]
import Counter from "./Counter.jsx";

function App() {
  return <Counter />;
}

export default App;
